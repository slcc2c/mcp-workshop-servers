# Memory MCP Configuration
# Set these before starting work sessions

# Current project name (use kebab-case)
MCP_PROJECT=mcp-server

# Current session date (YYYY-MM-DD)
MCP_SESSION=2025-06-28

# Optional: Memory data location (if you want separate files per project)
# MEMORY_DATA_PATH=./data/memory-${MCP_PROJECT}.json